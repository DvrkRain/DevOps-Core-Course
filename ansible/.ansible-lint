---
profile: basic

# Path-based role references are required locally because ansible.cfg is
# ignored in WSL (world-writable NTFS mount). CI works fine via ansible.cfg.
skip_list:
  - role-name[path]

warn_list:
  - internal-error
